<x-app-layout>
    <x-slot name="header"><div class="d-flex flex-wrap justify-content-between align-items-center gap-3"><div><p class="small text-success fw-bold mb-1">تفاصيل النتيجة</p><h1 class="h3 mb-0">{{ $result->student?->full_name }}</h1></div><a href="{{ route('results.index') }}" class="btn btn-outline-secondary">العودة</a></div></x-slot>
    <div class="container-fluid py-4 px-3 px-lg-4"><div class="row g-4"><div class="col-12 col-lg-5"><div class="card hafez-card border-0 h-100"><div class="card-body p-4"><h2 class="h5 mb-3">ملف الطالب</h2><dl class="mb-0"><dt>المسابقة</dt><dd>{{ $result->competition?->title }}</dd><dt>المستوى</dt><dd>{{ $result->competitionBranch?->name }}</dd><dt>نتيجة الطالب</dt><dd><span class="badge text-bg-{{ $result->result_status === 'successful' ? 'success' : 'danger' }}">{{ $result->result_status === 'successful' ? 'ناجح' : 'غير ناجح' }}</span></dd></dl></div></div></div><div class="col-12 col-lg-7"><div class="card hafez-card border-0"><div class="card-body p-4"><h2 class="h5 mb-4">النتيجة النهائية</h2><div class="row g-3"><div class="col-6"><div class="border rounded p-3"><small class="text-muted d-block">الدرجة النهائية</small><strong class="h3">{{ $result->final_score }}</strong></div></div><div class="col-6"><div class="border rounded p-3"><small class="text-muted d-block">النسبة</small><strong class="h3">{{ $result->percentage }}%</strong></div></div><div class="col-6"><div class="border rounded p-3"><small class="text-muted d-block">الترتيب</small><strong class="h3">{{ $result->rank ?? '—' }}</strong></div></div></div><div class="d-flex flex-wrap gap-2 mt-4">@if($result->certificates->isNotEmpty())<a class="btn btn-outline-success" href="{{ route('certificates.show',$result->certificates->first()) }}">عرض الشهادة</a>@else<form method="POST" action="{{ route('results.certificate',$result) }}" onsubmit="return confirm('سيتم إصدار شهادة تقدير لهذا الطالب. هل تريد المتابعة؟');">@csrf<button class="btn btn-success" type="submit">إصدار شهادة</button></form>@endif</div></div></div></div></div></div>
</x-app-layout>
