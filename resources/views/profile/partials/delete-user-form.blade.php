<section><header class="mb-3"><h2 class="h5 text-danger mb-2">منطقة الخطر</h2><p class="text-muted small mb-0">حذف الحساب نهائي، وسيؤدي إلى إزالة بيانات الحساب والموارد المرتبطة به.</p></header><button class="btn btn-outline-danger" x-data="" x-on:click.prevent="$dispatch('open-modal', 'confirm-user-deletion')">حذف الحساب</button>
    <x-modal name="confirm-user-deletion" :show="$errors->userDeletion->isNotEmpty()" focusable><form method="post" action="{{ route('profile.destroy') }}" class="p-4">@csrf @method('delete')<h2 class="h5">تأكيد حذف الحساب</h2><p class="text-muted small">أدخل كلمة المرور للتأكيد. لا يمكن التراجع عن هذه العملية.</p><label for="password" class="form-label">كلمة المرور</label><input id="password" name="password" type="password" class="form-control" required>@error('password','userDeletion')<div class="invalid-feedback d-block">{{ $message }}</div>@enderror<div class="d-flex justify-content-end gap-2 mt-4"><button type="button" class="btn btn-outline-secondary" x-on:click="$dispatch('close')">إلغاء</button><button class="btn btn-danger">حذف الحساب نهائياً</button></div></form></x-modal>
</section>
