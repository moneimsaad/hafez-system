@props(['action' => null, 'searchName' => 'search', 'searchValue' => null, 'placeholder' => 'بحث'])
<form method="GET" action="{{ $action ?? url()->current() }}" class="hafez-filter-bar row g-2 align-items-end mb-4"><div class="col-12 col-md-6"><label class="form-label" for="{{ $searchName }}">{{ $placeholder }}</label><input id="{{ $searchName }}" name="{{ $searchName }}" value="{{ $searchValue ?? request($searchName) }}" class="form-control" type="search"></div>{{ $slot }}<div class="col-12 col-md-auto"><div class="hafez-filter-actions"><button class="btn btn-success" type="submit">تطبيق</button><a class="btn btn-outline-secondary" href="{{ $action ?? url()->current() }}">مسح</a></div></div></form>
